# Order Service - Local configuration (will be overridden by config-server)
# This file can be used for local development overrides

spring:
  kafka:
    bootstrap-servers: localhost:9092
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
      properties:
        spring.json.trusted.packages: "com.ecommerce.orderservice.dto"
        spring.json.use.type.headers: false
    properties:
      security.protocol: PLAINTEXT

# Uncomment and modify for local development if needed
#   datasource:
#     url: jdbc:mysql://localhost:3306/ecommerce_orderdb?createDatabaseIfNotExist=true
#     username: root
#     password: drowssap

# Kafka topic configuration
kafka:
  topic:
    notifications: notifications